package com.proot.cowork.ui.design

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp
import androidx.compose.ui.unit.sp

object CoworkTokens {
    val Bg = Color(0xFF0A0A0B)
    val Surface = Color(0xFF141416)
    val SurfaceElevated = Color(0xFF1C1C1F)
    val Border = Color(0xFF2A2A2E)
    val Mint = Color(0xFF5EEAD4)
    val UbuntuOrange = Color(0xFFE95420)
    val TextPrimary = Color(0xFFF8FAFC)
    val TextSecondary = Color(0xFF94A3B8)
    val TextMuted = Color(0xFF64748B)
    val SpeakBg = Color(0xFFFFFFFF)
    val SpeakFg = Color(0xFF0A0A0B)
    val Pending = Color(0xFFFBBF24)
    val Done = Color(0xFF4ADE80)
    val Failed = Color(0xFFF87171)
    val FolderAccent = Color(0xFFFACC15)
    val FileAccent = Color(0xFF60A5FA)
    val TerminalPrompt = Color(0xFF5EEAD4)
    val DesktopTitleBar = Color(0xFF1E293B)

    val RadiusCard = 16.dp
    val RadiusComposer = 24.dp
    val RadiusPill = 20.dp
    val RadiusIconTile = 12.dp

    val ShapeCard = RoundedCornerShape(RadiusCard)
    val ShapeComposer = RoundedCornerShape(RadiusComposer)
    val ShapePill = RoundedCornerShape(RadiusPill)
    val ShapeIconTile = RoundedCornerShape(RadiusIconTile)

    val DesktopHeightChat = 210.dp
    val DesktopHeightDefault = 180.dp
    val DesktopHeightTerminal = 132.dp
    val NavHeight = 58.dp

    val NavLabelSize = 10.sp
    val SectionLabelSize = 11.sp
}
